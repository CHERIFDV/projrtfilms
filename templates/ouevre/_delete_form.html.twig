<form method="post" action="{{ path('app_ouevre_delete', {'id': ouevre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ouevre.id) }}">
    <button class="btn">Delete</button>
</form>
